name: frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
   
  syncfusion_flutter_pdf: ^25.1.35   # PDF page counter
  razorpay_flutter: ^1.3.6           # Payment gateway
        # File upload (you may already have this)
  intl: ^0.19.0                      # ₹ currency format
  cupertino_icons: ^1.0.8
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  firebase_storage: ^13.2.0
  google_fonts: ^6.2.1
  provider: ^6.1.2
  animate_do: ^3.3.4
  intl_phone_field: ^3.2.0
  pinput: ^5.0.0
  mailer: ^6.1.0          # ← NEW: for sending email OTP
  shared_preferences: ^2.2.3  # ← NEW: store OTP temporarily
  http: ^1.2.1    # ← Add this
  file_picker: ^8.1.2
  permission_handler: ^11.3.1 
  device_info_plus: ^10.1.0  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true